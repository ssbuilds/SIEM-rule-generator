# Database Configuration (Optional)
# If not provided, the application will use in-memory storage
DATABASE_URL=postgresql://username:password@localhost:5432/siem_rules

# PostgreSQL Connection Details (Alternative to DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=siem_rules

# AI Provider API Keys (Configure via UI - these are examples)
# Do not commit actual API keys to version control
# ANTHROPIC_API_KEY=sk-ant-api03-...
# OPENAI_API_KEY=sk-...
# GROQ_API_KEY=gsk-...

# Application Configuration
NODE_ENV=development
PORT=5000